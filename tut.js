const redius = [3, 1, 2, 4];
const areasOfCircles = redius.map((items) => Math.PI * items * items);
console.log(areasOfCircles);
const circonferences = redius.map((items) => 2 * (Math.PI * items));
console.log(circonferences);
